# src/shared

Shared runtime building blocks used across features.

```
shared/
├── core/       import, runtime, platform
└── ui/         terminal, output, prompts, stepAnimation
```

See `src/ARCHITECTURE.md` for the broader map.
