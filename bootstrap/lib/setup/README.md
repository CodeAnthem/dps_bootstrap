# setup

Legacy helpers retained for compatibility (`_nixosSetup`).

| File | Role |
|------|------|
| `disk.sh` | Older disk variable shims |
| `network.sh` | Live-ISO network helpers |
| `installation.sh` | Classic install hooks |

Prefer `installer/` and `partition/` for new work.
